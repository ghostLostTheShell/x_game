extends Sprite2D


func _ready() -> void:
	#await get_tree().create_timer(1).timeout
	##var tween = get_tree().create_tween()
	##tween.tween_property(self, "modulate", Color(1, 1, 1, 1), 2)  # 1秒内透明
	##await tween.finished
	#queue_free()
	pass
